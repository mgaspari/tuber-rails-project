<style media="screen">
  .tubing_group_list {
    background-color: #61F73F;
    color: black;
    border-radius: 7px;
  }
</style>


<div>
  <ul>
    <% @notes = Video.find(session[:vid_id]).notes %>
    <% sleep(0.7) %>
    <% @notes.each do |note| %>
      <li style="list-style:none;"><%= note.title %> : <%= note.content %></li>
    <% end %>
  </ul>
</div>
